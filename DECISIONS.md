# DECISIONS

Every call made where the PRD or the build goal left room for judgment. Newest
section last. Each entry: what was decided, what it overrides, and what would
have to change to revisit it.

Repo: `byom` · Build run started 2026-08-11 · unattended

---

## D-001 — Use the official SDK for transport, not a hand-rolled WebSocket

**Decided:** Depend on `@deepgram/sdk` ^5.7.0 and issue Speak through
`client.speak.v2.createConnection({ model: 'flux-rufus-en' })`.

**Overrides:** PRD §5.2 — "Node 22+, zero runtime dependencies. Uses the global
`WebSocket`." The repo now has exactly one runtime dependency.

**Why:** The build goal's SDK USAGE rule is explicit — use the official SDK for
anything it already covers (issuing Speak, reading Warning/SpeechMetadata
frames, Flush/Interrupt) and hand-roll only for genuine gaps, after checking the
current version. Verified against SDK v5.7.0 (latest published, 2026-08-04):

- `src/CustomClient.ts:1609-1674` — `SpeakV2ClientWrapper`, `urlPath: "/v2/speak"`
- Binary audio-frame handling already patched over the autogenerated JSON-only
  Socket class, which is the hard part of this transport
- `src/api/types/Deepgram.ts` — full `flux-*` voice enum

PRD §5.2's zero-dependency stance predates this SDK surface existing. PRD §5.5
already conceded `ws` as "the one dependency this project would ever need"; the
official SDK is the better version of that concession, because it also tracks
frame-shape changes for us through GA.

**Revisit if:** the SDK's v2 speak surface is withdrawn or materially changes
before GA, or a bug in the wrapper's binary handling forces a raw socket. See
SDK_WATCH.md for the gaps still hand-rolled around.

---

## D-002 — Staging base URL is a required injected config with no production default

**Decided:** `DEEPGRAM_BASE_URL` is read from env with **no fallback**. If unset,
the server refuses to start rather than defaulting to anything. Mocked tests
inject a loopback URL.

**Context:** `DEEPGRAM_STAGING_API_KEY` is present in this environment. A staging
*host* is not — nothing in the env supplies one, and PRD §3/§5.2 name
`wss://api.deepgram.com/v2/speak`, which is production.

**Why no default:** The goal forbids building a convenience path that makes a
production flip easy by accident. A default value is exactly that path. Failing
closed on missing config costs one clear error message and removes the failure
mode entirely.

**Consequence:** Live verification against staging is halted (FLAGS.md F-001).
Step 1's acceptance gate runs against a mocked `/v2/speak` and is unaffected.

**Revisit if:** a staging host is supplied. Nothing in the code changes — set the
env var.

---

## D-003 — Repo location and name

**Decided:** `~/code/byom`, public GitHub repo `dg-coreylweathers/byom`.

**Why:** PRD §5.5 decides the public name ("Bring Your Own Markup" / BYOM in all
code, repo name, and public copy; "paste toy" stays internal shorthand), and the
goal says treat the PRD's own recommendations as decided rather than open.
`~/code/` is where every other repo on this machine lives. Public because the
goal defaults to public and nothing here argues otherwise — the Champion persona
piece (PRD §7) is a build teardown that requires a forkable public repo to exist.

---

## D-004 — Fly.io deploy target: personal org

**Decided:** Deploy to the `personal` Fly org, not the shared `deepgram` org.

**Why:** PRD §5.5 explicitly declines to guess the deploy target and says ask.
No one is available to ask, and the goal's rule for decisions that depend on
something outside this repo is to take the most conservative reasonable
placeholder and keep moving. Shipping a launch-week diagnostic into the shared
company org is the higher-consequence guess of the two; personal is reversible
by redeploy.

**Marked as placeholder.** Routed to a human in FLAGS.md (F-002) alongside the
rest of the hosting decision — CDN, restricted TTS-only key, spend cap.

---

## D-005 — Skill substitutions (two of three named skills are not installed)

The goal names three skills. Only one exists in this environment.

| Named in goal | Reality | Action |
|---|---|---|
| `devrel-code-review` | Not installed | Ran `devrel-review` — the only installed DevRel skill, and its description covers SDK/starter-app code review |
| `deepgram-devrel-drafting` | Not installed | Wrote PRD §6/§7 content directly against the PRD's own constraints and voice rules |
| `skill-creator` | Not installed | No skill was created or extended — the mechanism the goal prescribes for closing skill gaps is unavailable |

**Consequence:** The goal's instruction to extend a skill with `skill-creator`
when it lacks coverage could not be carried out. No skill files were modified in
this run. If a skill gap generalizes, it needs creating in a session where
`skill-creator` is present. FLAGS.md F-003.

---

## D-006 — Default voice `flux-rufus-en`, `flux-marcus-en` rejected server-side

**Decided:** As PRD §5.3/§5.4 require. Independently verified rather than taken
on faith, because PRD §8 flags the underlying guidance as suspect.

**Verification** — the shipping catalog in SDK v5.7.0 `src/api/types/Deepgram.ts`
is 12 voices: alexis, bruce, cole, drew, haley, heather, jack, marcus, priya,
renee, rufus, sharon.

Launch WER guidance recommends meghan/rufus/conor/wes and says avoid
brittany/marcus. Cross-referenced: **meghan, conor, wes, and brittany do not
exist in the shipping catalog at all.** Only rufus and marcus do, and marcus is
the one with reported defects.

So `flux-rufus-en` is the only name on both the preferred-WER list and the
shipping catalog — the PRD's claim is exactly right, and it is right by
elimination across a catalog where 4 of 6 guidance names are phantoms. That
makes the guidance itself the problem, not the default. Escalated as FLAGS.md
F-004 with this evidence attached; not resolved here, per PRD §8.

---

## D-007 — Build a reference upstream rather than deploy against an injected double

**Decided:** `tools/reference-upstream.js` implements the server side of
`/v2/speak` as a standalone service. BYOM connects to it over a real WebSocket
through the real SDK.

**Why:** Directed during the run, and it resolves a genuine problem with the
alternative. With no staging host (F-001), the only other option was the
in-process double injected at the SDK-client boundary — which leaves the entire
transport untested: `createConnection`, header auth, binary frame delivery, and
protocol ordering as the SDK actually parses it. A deploy on that basis would be
a shell.

Building it real immediately paid for itself. Three defects were found that the
injected double could not have surfaced, all in SDK_WATCH: audio arriving as
`Blob` (W-004), `sendSpeak`/`sendFlush` needing an explicit `type` (W-005), and
BYOM misreading an absent `Warning` frame as a failure to report rather than as
confirmation that nothing was stripped.

**Honesty constraints, since it is not a synthesizer:**

- It announces itself in `SessionMetadata.implementation`. BYOM reads that and
  labels the receipt `origin: "reference"` — never `"api"` — with a note saying
  the figures are not API-reported and not a real synthesis. This follows the rule
  PRD §3 already sets for the batch fallback: a number that did not come from the
  API is visibly marked.
- It shares `public/markup.js` with BYOM rather than restating the strip contract.
  A reference implementation of a contract should not fork it. Consequence: mirror
  and upstream agree by construction, which is correct. `--fault` forces a
  disagreement so BYOM's disagreement flag can be exercised on a live connection.
- It deliberately reproduces both PRD §5.3 output defects — ~350ms of head dead
  air, and output normalized to exactly full scale with no headroom. A
  well-behaved mock would hide the two things this tool exists to demonstrate.
  Full-scale peak is reached by normalizing rather than by a fixed gain, because
  summed harmonics only reach unity when phases align, which would otherwise
  leave incidental headroom that varies with word count.

**Revisit if:** a staging host appears. The upstream stays useful for CI and
offline demos, but the deployed tool should point at staging, and the receipt
label will switch from `reference` to `api` on its own with no code change.

---

## D-008 — PRD §5.5's WebSocket-auth open decision is closed by the SDK

**Decided:** No action needed. The auth question in PRD §5.5 does not arise.

**PRD §5.5 said:** the build uses the `token` subprotocol because "Node's global
`WebSocket` can't set headers," and if deployment requires an `Authorization`
header instead, `lib/flux.js` would need the `ws` package added — "the one
dependency this project would ever need."

**What is actually true.** Verified by probing the handshake: the SDK sends an
`Authorization` header and no subprotocol at all. It loads `ws` internally
(`loadNodeWebSocket()`), so the header limitation of the global `WebSocket` never
applies.

So both branches of §5.5's contingency are moot — the header path is already what
ships, and we do not need to add `ws` for it. `lib/flux.js` remains the only file
that would change if auth ever changes, so the PRD's structural claim holds even
though its premise does not.

**Note on `ws`:** it *is* now a declared dependency, but for an unrelated reason —
`tools/reference-upstream.js` needs a WebSocket **server**, which Node has no
built-in for. It was already present as an SDK transitive dependency; declaring it
explicitly avoids depending on someone else's dependency tree.
